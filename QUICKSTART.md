# 快速开始

```powershell
cd E:\Project\Common\AutoKey-Rust
cargo build --release
.\target\release\autokeyrust.exe
```

在界面中点击按键按钮捕获目标键，设置间隔后点击“启动”。左 Alt 可切换启动/停止，`Ctrl+Alt+Space` 可绑定鼠标所在窗口。

完整说明见 [README.md](README.md)。
